# to force display kernel selector on vscode and choose usr/bin source
# fix PATH export PATH=/home/<user>/.local/bin:$PATH

import pandas as pd
import qrcode.image.svg
from PIL import Image, ImageDraw, ImageFont
from cairosvg import svg2png
